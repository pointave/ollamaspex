This project connects your ollama models to Windows screenshot. The default shortcut in Windows is win+shift+S, you'll then select the region you capture and the program will appear where you can ask 
your question.

1.git clone repo
2.conda create -n ollamaspex python=3.11
3. pip install -r requirements.txt
